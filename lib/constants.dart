import 'package:flutter/material.dart';

const kKeyButtonTextStyle = TextStyle(
  fontSize: 40.0,
  color: Colors.black,
);

const kKeyButtonPadding = EdgeInsets.all(10.0);
const kKeyButtonFillColor = Colors.grey;
const kKeyButtonShape = CircleBorder();
const kKeyButtonConstraint = BoxConstraints(
  minWidth: 65.0,
  minHeight: 65.0,
  maxHeight: 100.0,
  maxWidth: 100.0,
);
const kResultTextStyle = TextStyle(fontSize: 40.0,);
const kInputTextStyle = TextStyle(fontSize: 40.0,);
const kInputResultTextPadding = EdgeInsets.fromLTRB(10.0, 0, 10.0, 0);
